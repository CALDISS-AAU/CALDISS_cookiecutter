[project]
name = "{{ cookiecutter.project_slug }}"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
create-pipeline = "Shared_Functions.pipeline_generator:main"

[tool.setuptools]
packages = ["Shared_Functions"]